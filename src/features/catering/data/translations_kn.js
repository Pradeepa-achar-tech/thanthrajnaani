// Kannada localization overrides — Catering Management course.
// Stub for now (English-only per locked decision §12). When step 10 of the
// build sequence runs, fill these maps following the flutter feature's pattern:
// English-first mix with Kannada only as connector/explanation language;
// keep code, package names, and Claude Code prompts in English.

export const moduleCopyKn = {}
export const sectionTitleKn = {}

export const kn = { topics: {} }

export function localizeLessonText(text) {
  return text
}

export function localizeTopicLesson(topic) {
  return topic
}
