// Kannada localization overrides — Temple Seva Desktop App course.
// English-only for now. When a Kannada pass runs, fill these maps following the
// flutter feature's pattern: English-first mix with Kannada only as
// connector/explanation language; keep code, package names, and Claude Code
// prompts in English.

export const moduleCopyKn = {}
export const sectionTitleKn = {}

export const kn = { topics: {} }

export function localizeLessonText(text) {
  return text
}

export function localizeTopicLesson(topic) {
  return topic
}
